
post_install() {
cd /usr/release-files
mv /etc/arch-release                 /etc/harch-release
cp "lsb-release"                    "/etc"
cp "os-release"                     "/etc"
rm -fr /usr/release-files
}

